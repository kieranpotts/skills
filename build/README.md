Artifacts compiled by the `./run/install` script will be saved to this
directory. The artifacts are excluded from source control, but they should not
be deleted, since some may be the targets of symlinks from the user's home
directory and project directories.
