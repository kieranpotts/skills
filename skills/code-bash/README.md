# Bash skill

Invoke this skill when writing or modifying Bash scripts to ensure the agent applies correct Bash-specific idioms - strict mode, `local` variables, `[[ … ]]` conditionals, arrays, and arithmetic - rather than falling back to generic POSIX patterns that miss Bash's safety features.
