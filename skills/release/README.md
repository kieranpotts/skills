# Releases skill

Invoke this skill when preparing a release to ensure branches and tags follow the project's conventions and the release process is followed correctly - cuts from the right trunk, no fixes committed directly to release branches, artifacts kept out of the repository.
