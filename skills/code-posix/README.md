# POSIX skill

Invoke this skill when writing or modifying shell scripts that must run across multiple shells or platforms. It ensures the agent stays within the POSIX baseline and avoids shell-specific extensions that would silently fail in environments where only `sh` is guaranteed.
